# Database
DATABASE_URL="postgresql://user:password@localhost:5432/airman?schema=public"

# JWT (use a strong secret in production; same key for signing and verification)
JWT_PUBLIC_KEY="your-256-bit-secret-at-least-32-chars"
JWT_ACCESS_EXPIRES_SEC=900
JWT_REFRESH_EXPIRES_DAYS=7

# Redis (for BullMQ)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# App
PORT=3001
FRONTEND_ORIGIN=http://localhost:3000
