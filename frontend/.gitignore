# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables – never commit secrets
.env
.env.local
.env.*.local
# Keep the example template tracked
!.env.example

# Prisma migrations MUST be committed so `prisma migrate deploy`
# can apply them in Docker / CI without generating new ones.
# Do NOT add prisma/migrations here.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
